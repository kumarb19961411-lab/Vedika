rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    match /vendors/{vendorId}/{allPaths=**} {
      // Gallery & Profile Assets
      // Enable public read for discovery
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == vendorId;
    }
  }
}
